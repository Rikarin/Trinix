TRINIX_VERSION = 0.0.1
RC_VERSION = 1

ifeq ($(BUILD_TYPE),)
	TRINIX_VERSION := $(TRINIX_VERSION)-alpha
endif

ifeq ($(BUILD_TYPE),rc)
	TRINIX_VERSION := $(TRINIX_VERSION)-rc$(RC_VERSION)
endif

ifeq ($(BUILD_TYPE),release)
	TRINIX_VERSION := $(TRINIX_VERSION)
endif
TRAVIS_BUILD_NUMBER ?= 0
SYSTEM_VERSION = 0.1.${TRAVIS_BUILD_NUMBER}
RC_VERSION     = 0


ifeq ($(BUILD_TYPE),rc)
	SYSTEM_VERSION := $(SYSTEM_VERSION)-rc$(RC_VERSION)
endif

ifeq ($(BUILD_TYPE),release)
	SYSTEM_VERSION := $(SYSTEM_VERSION)
endif
